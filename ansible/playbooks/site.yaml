---
- name: 🚀 Full Jenkins Infrastructure Setup
  hosts: all
  gather_facts: yes
  become: yes

  pre_tasks:
    - name: 💡 Display start info
      debug:
        msg: "Starting full environment setup for {{ inventory_hostname }}"

- import_playbook: devops_user_setup.yaml
- import_playbook: update_hosts.yaml
- import_playbook: master_setup.yaml
- import_playbook: worker_setup.yaml
- import_playbook: register_worker.yaml

- name: ✅ Final Summary
  hosts: all
  gather_facts: no
  tasks:
    - name: Display completion message
      debug:
        msg: "🎉 Jenkins environment configured successfully on {{ inventory_hostname }}"
